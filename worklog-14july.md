# Worklog - 14 July

## Features Worked On

- Payment method editor: icon upload/selection, side-by-side uploaded icons, and soft-delete flow.
- Add funds page: compact UI refresh, gateway tile redesign, amount layout cleanup, and bonus/fee breakdown display.
- Payment methods page: removed the slow AJAX list flow and fixed the server-rendered page load path.
- SMS helper: added `hosting-sms-creds.php` to fetch SMS settings from the `settings` table as JSON for deploy/ops use.

